fn @putint(#x: i32) -> ();
fn @putch(#x: i32) -> ();
fn @putarray(#n: i32, #arr: i32*) -> ();
fn @getint() -> i32;
fn @getch() -> i32;
fn @getarray(#n: i32, #arr: i32*) -> ();
fn @whileIf() -> i32 {
%entry:
  let %ret_val.addr = alloca i32, 1
  let %a = alloca i32, 1
  let %0 = store 0, %a
  let %b = alloca i32, 1
  let %1 = store 0, %b
  jmp label %while_cond
%while_cond:
  let %3 = load %a
  let %4 = lt %3, 100
  br %4, label %while_body, label %while_end
%while_body:
  let %6 = load %a
  let %7 = eq %6, 5
  br %7, label %if_then, label %if_else
%while_end:
  let %9 = load %b
  let %10 = store %9, %ret_val.addr
  jmp label %exit
%if_then:
  let %12 = store 25, %b
  jmp label %if_end
%if_end:
  let %14 = load %a
  let %15 = add %14, 1
  let %16 = store %15, %a
  jmp label %while_cond
%if_else:
  let %18 = load %a
  let %19 = eq %18, 10
  br %19, label %if_then.1, label %if_else.1
%if_then.1:
  let %21 = store 42, %b
  jmp label %if_end.1
%if_end.1:
  jmp label %if_end
%if_else.1:
  let %24 = load %a
  let %25 = mul %24, 2
  let %26 = store %25, %b
  jmp label %if_end.1
%after_return:
  jmp label %exit
%exit:
  let %29 = load %ret_val.addr
  ret %29
}
fn @main() -> i32 {
%entry:
  let %ret_val.addr = alloca i32, 1
  let %0 = call @whileIf
  let %1 = call @putint, %0
  let %2 = store 0, %ret_val.addr
  jmp label %exit
%after_return:
  jmp label %exit
%exit:
  let %5 = load %ret_val.addr
  ret %5
}

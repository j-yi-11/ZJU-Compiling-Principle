fn @putint(#x: i32) -> ();

fn @putch(#x: i32) -> ();

fn @putarray(#n: i32, #arr: i32*) -> ();

fn @getint() -> i32;

fn @getch() -> i32;

fn @getarray(#n: i32, #arr: i32*) -> ();

fn @f() -> i32 {
%entry:
  let %ret_val.addr: i32* = alloca i32, 1
  let %3: () = store 10, %ret_val.addr: i32*
  jmp label %exit
%after_return:
  jmp label %exit
%exit:
  let %6: i32 = load %ret_val.addr: i32*
  ret %6: i32
}

fn @main() -> i32 {
%entry:
  let %ret_val.addr: i32* = alloca i32, 1
  let %3: i32 = call @f
  let %4: () = call @putint, %3: i32
  let %5: () = store 0, %ret_val.addr: i32*
  jmp label %exit
%after_return:
  jmp label %exit
%exit:
  let %8: i32 = load %ret_val.addr: i32*
  ret %8: i32
}


fn @putint(#x: i32) -> ();

fn @putch(#x: i32) -> ();

fn @putarray(#n: i32, #arr: i32*) -> ();

fn @getint() -> i32;

fn @getch() -> i32;

fn @getarray(#n: i32, #arr: i32*) -> ();

fn @if_ifElse_() -> i32 {
%entry:
  let %ret_val.addr: i32* = alloca i32, 1
  let %a: i32* = alloca i32, 1
  let %4: () = store 5, %a: i32*
  let %b: i32* = alloca i32, 1
  let %6: () = store 10, %b: i32*
  let %7: i32 = load %a: i32*
  let %8: i32 = eq %7: i32, 5
  br %8: i32, label %if_then, label %if_end
%if_then:
  let %11: i32 = load %b: i32*
  let %12: i32 = eq %11: i32, 10
  br %12: i32, label %if_then.1, label %if_else
%if_end:
  let %20: i32 = load %a: i32*
  let %21: () = store %20: i32, %ret_val.addr: i32*
  jmp label %exit
%if_then.1:
  let %16: () = store 25, %a: i32*
  jmp label %if_end.1
%if_end.1:
  jmp label %if_end
%if_else:
  let %17: i32 = load %a: i32*
  let %18: i32 = add %17: i32, 15
  let %19: () = store %18: i32, %a: i32*
  jmp label %if_end.1
%after_return:
  jmp label %exit
%exit:
  let %24: i32 = load %ret_val.addr: i32*
  ret %24: i32
}

fn @main() -> i32 {
%entry:
  let %ret_val.addr: i32* = alloca i32, 1
  let %3: i32 = call @if_ifElse_
  let %4: () = call @putint, %3: i32
  let %5: () = store 0, %ret_val.addr: i32*
  jmp label %exit
%after_return:
  jmp label %exit
%exit:
  let %8: i32 = load %ret_val.addr: i32*
  ret %8: i32
}


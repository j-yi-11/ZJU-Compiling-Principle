fn @putint(#x: i32) -> ();

fn @putch(#x: i32) -> ();

fn @putarray(#n: i32, #arr: i32*) -> ();

fn @getint() -> i32;

fn @getch() -> i32;

fn @getarray(#n: i32, #arr: i32*) -> ();

fn @main() -> i32 {
%entry:
  let %ret_val.addr: i32* = alloca i32, 1
  let %a: i32* = alloca i32, 1
  let %4: () = store 10, %a: i32*
  let %5: i32 = load %a: i32*
  let %6: i32 = rem %5: i32, 3
  let %7: () = call @putint, %6: i32
  let %8: () = store 0, %ret_val.addr: i32*
  jmp label %exit
%after_return:
  jmp label %exit
%exit:
  let %11: i32 = load %ret_val.addr: i32*
  ret %11: i32
}

